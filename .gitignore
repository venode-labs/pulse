# Editor + OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Build / package output (reserved for the Go rewrite)
/build/
/dist/
/pulse
/pulse-collector
*.tar.gz
*.tar.zst
*.pkg.tar.zst

# Live state (operators should never check this in)
state/
*.log

# Local config overrides
config/config.toml
!config/config.toml.example
