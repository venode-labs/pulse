# Allow QML XMLHttpRequest to read local files (Qt 6 default is deny).
# Needed so the Security Pulse plasmoid can read the collector's JSON
# at ~/.local/state/pulse/. Scope: this user, all services
# launched via systemd user manager (which is how plasmashell starts).
QML_XHR_ALLOW_FILE_READ=1
